import { EntityResponse } from "../../EntityResponse";
import { Token } from "../../Token";

export class Institution {
  Id: number;
  Name: string;
  AdminId: string;
  IsActive: boolean;
  CreatedDate: string;
  UpdatedDate: string;
}
