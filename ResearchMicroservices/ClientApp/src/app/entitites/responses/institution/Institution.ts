import { EntityResponse } from "../../EntityResponse";

export class Institution {
  Id: number;
  Name: string;
  AdminId: string;
  IsActive: boolean;
  CreatedDate: string;
  UpdatedDate: string;
}
